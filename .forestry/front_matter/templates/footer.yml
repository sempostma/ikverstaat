---
label: Footer
hide_body: false
fields:
- type: field_group_list
  name: pages
  label: Pagina's
  description: De links in de footer.
  fields:
  - type: text
    name: title
    label: Titel
    description: De titel van de link
    config:
      required: true
  - type: text
    name: url
    label: url
    description: 'De url van de pagina. Bijvoorbeeld: "/projecten"'
    config:
      required: true
  config:
    labelField: title
pages:
- _data/footer.yml
